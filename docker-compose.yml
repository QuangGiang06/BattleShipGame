services:
  battleship:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      CORS_ORIGIN: http://localhost:3000
      EXPOSE_ROOMS: "false"
      RECONNECT_GRACE_MS: 30000
    restart: unless-stopped
